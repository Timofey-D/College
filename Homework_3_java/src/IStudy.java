/**
 * The interface is implemented by classes, which are represented like students.
 * */
public interface IStudy {
    /**
     * The method checks whether a student has a course, which is specify.
     * */
    boolean checkStudyCourses(String course);
}
